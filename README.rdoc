rake db:create
rake db:migrate
rails s

AngularJS + Rails

Took some of the methods inside the controllers and switched to GRAPE to make requests through APIs. 

TODO: Extract completely Angular js files to a completely new folder (not project)and use gulp and bower for depencies management for the frontend.
